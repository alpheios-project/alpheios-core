import Vue from 'vue/dist/vue' // Vue in a runtime + compiler configuration

export default class WordlistController {
  constructor () {
    this.test = 'Test'
    console.info('*******************word list controller constructor')
  }
}